
export const handleProjectEdit = (projectId) => {
    console.log(projectId);
};

export const handleExperienceEdit = (experienceId) => {
    console.log(experienceId);
};

export const handleProjectDelete = (projectId) => {
    console.log(projectId);
};

export const handleExperienceDelete = (experienceId) => {
    console.log(experienceId);
};

